data:
  corpus_path: artifacts/sample_msmarco_passage/corpus.jsonl
  queries_path: artifacts/sample_msmarco_passage/queries.dev.jsonl
  candidate_run_path: results/sample_cross_encoder_reranked_run.json

model:
  model_name_or_path: sentence-transformers/all-MiniLM-L6-v2
  max_length: 128
  query_prefix: ""
  passage_prefix: ""

inference:
  batch_size: 8
  top_k: 5
  use_cpu: true
  run_output_path: results/sample_colbert_reranked_run.json
