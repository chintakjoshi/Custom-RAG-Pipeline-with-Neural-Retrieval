data:
  pairs_path: artifacts/sample_msmarco_passage/cross_encoder_train_pairs.jsonl
  output_path: artifacts/sample_msmarco_passage/distillation_soft_labels.jsonl

teacher_model:
  model_name_or_path: checkpoints/sample_cross_encoder
  max_length: 256
  num_labels: 1

inference:
  batch_size: 8
  use_cpu: true
  show_progress_bar: true
